#include <stdio.h>

int main()
{
    int i = 0, Count = 0;
    printf("Enter a number of times the loop need to run : \n");
    scanf("%d", &Count);

    for (i= 1; i<= Count; i++)
    {
        printf("Jay Ganesh...\n");
    }
    
    return 0;
}